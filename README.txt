Ernel Murati
August 19, 2016
Introduction


Testing Git commands on local repository to be connected to my devbox.

$ git			- Check if git runs on the machine
$ git init		- To make a directory your repository
$ git status		- Check the status of the repository
“Untracked Files”	- It means that git does not control the files, invisble to it
$ git add		- Add a file into a temp bucket before saving the changes
$ git commit -m	 “…”	- Commit to a project and adding comments
$ git log		- Keeps a log of the changes, author, date and note of commit
